apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: component-docs
  description: A test document to check git repos caching in backstage
  labels:
    system: public-websites
  annotations:
    backstage.io/techdocs-ref: dir:./
  tags:
    - java
    
spec:
  type: service
  lifecycle: production
  owner: nidhi-sinha
